// util.h file
#ifndef UTIL_H
#define UTIL_H

#include <stdio.h>
#include <string.h>

// Function to remove trailing newline character from a string.
// It is often used when reading input from the user to remove the newline character that gets appended at the end.
void chomp(char *str);

#endif /* UTIL_H */
